services:
  dashboard:
    build: .
    container_name: powerlifting_dashboard
    ports:
      - "8501:8501"
    volumes:
      # Live-mount code and data so edits show up without a rebuild
      - ./src:/app/src
      - ./Aayush man .xlsx:/app/Aayush man .xlsx
